name: Update Predictions

on:
  schedule:
    # 매일 오전 9시 (KST 기준, UTC 00:00)
    - cron: '0 0 * * *'
    # 매일 오후 6시 (KST 기준, UTC 09:00) - 저녁 경기 대비
    - cron: '0 9 * * *'
  workflow_dispatch: # 수동 실행 허용
    inputs:
      force_update:
        description: '강제 업데이트 (기존 예측 덮어쓰기)'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  update-predictions:
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.check_changes.outputs.has_changes }}
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🌐 Install Chrome dependencies (for Puppeteer)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 \
            libnspr4 \
            libatk-bridge2.0-0 \
            libdrm2 \
            libxkbcommon0 \
            libgbm1 \
            libasound2t64 || sudo apt-get install -y libasound2

      - name: 🔍 Scrape match schedule
        run: npm run scrape
        continue-on-error: true

      - name: 🤖 Generate AI predictions
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: npm run predict

      - name: 📊 Check for changes
        id: check_changes
        run: |
          if git diff --quiet src/data/predictions.json; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "📭 변경사항 없음 - 동일한 회차"
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "📬 새로운 회차 또는 예측 업데이트 감지"
          fi

      - name: 🏗️ Build project
        if: steps.check_changes.outputs.has_changes == 'true' || github.event.inputs.force_update == 'true'
        run: npm run build

      - name: 📝 Commit updated predictions
        if: steps.check_changes.outputs.has_changes == 'true' || github.event.inputs.force_update == 'true'
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add src/data/predictions.json
          git diff --staged --quiet || git commit -m "[웹개발유닛][chore] (prediction): 예측 데이터 자동 업데이트

          - 축구토토/농구토토 경기 일정 크롤링
          - Gemini AI 예측 생성"
          git push

      - name: 📤 Upload artifact for Pages
        if: steps.check_changes.outputs.has_changes == 'true' || github.event.inputs.force_update == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: update-predictions
    if: needs.update-predictions.outputs.has_changes == 'true' || github.event.inputs.force_update == 'true'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
